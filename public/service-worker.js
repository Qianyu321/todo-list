self.addEventListener("install", (event) => {
    console.log("Service worker installing...");
    // Here, you'll later add code for actions like caching assets.
    //use the event hander here acccordingly of what you want to implent.
});